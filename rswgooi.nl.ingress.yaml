apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rswgooi-nl
  namespace: redirector
spec:
  ingressClassName: nginx
  rules:
    - host: rswgooi.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 8080
    - host: www.rswgooi.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 8080
